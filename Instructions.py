class instructions:
    def __init__(self, range):
        self.output = []
        self.range = range

    def print():
        for i in range(0, len(self.output)):
            print(self.output[i])
